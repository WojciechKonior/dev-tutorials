#include <stdio.h>
#include <stdlib.h>

int main()
{
  system("ls");
  system("echo 'some content' > test.txt");
  system("cat test.txt");
  system("abc") !=0)
    printf("\nError executing command.\n");
  return 0;
}
